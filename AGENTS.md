# Agent 工作准则 / Agent Workspace Rules

## 核心指令 (Mandatory Directives)
1. **每次处理都需要更新 Git**：
   - 只要进行了代码编写、Bug 修复、算法调优或参数调整，必须执行 Git 提交（git add & git commit）。
   - 提交信息必须规范、清晰地说明修复内容与原理。
2. **工程与算法严谨性**：
   - 保持软硬件几何视差规律与算法边界逻辑一致。
   - 每次 C++/Java 核心逻辑修改后必须验证编译，并同步更新 Git 状态。
